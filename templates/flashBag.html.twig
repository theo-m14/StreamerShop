{% for message in app.flashes('success') %}
	<div class="flash-success has-text-centered">
		{{ message }}
	</div>
{% endfor %}
{% for message in app.flashes('error') %}
	<div class="flash-error has-text-centered">
		{{ message }}
	</div>
{% endfor %}
