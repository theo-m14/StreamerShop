<nav class="main-nav">
    <a href="{{ path('app_home') }}">
        {# <img src="/images/logo.png" alt="Logo"> #}
        <h2>Logo</h2>
    </a>
    <ul>

        {% if app.user %}
            {% if is_granted('ROLE_ADMIN') %}
                <li><a href="{{ path('app_admin_users') }}">Admin</a></li>
            {% endif %}
            <li><a href="{{ path('app_product_index') }}">Gestion boutique</a></li>
            <li><a href="{{ path('app_shop', {'username': app.user.username}) }}">Ma boutique</a></li>
            <li><a href="{{ path('app_account') }}">Mon compte</a></li>
            <li><a href="{{ path('app_logout') }}">Déconnexion</a></li>
        {% else %}
            <li><a href="{{ path('app_login') }}">Connexion</a></li>
            <li><a href="{{ path('app_register') }}">Inscription</a></li>
        {% endif %}
    </ul>
</nav>